server {
    listen 80;

    location / {
        auth_basic "Restricted Dashboard";
        auth_basic_user_file /etc/nginx/.htpasswd;

        proxy_pass https://metabase-production-ca54.up.railway.app/
    }
}
